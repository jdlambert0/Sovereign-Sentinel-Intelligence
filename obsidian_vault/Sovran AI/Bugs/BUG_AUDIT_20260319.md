# Bug Audit - 2026-03-19 (Pre-Market)

## Status: CRITICAL FIXES REQUIRED
**Date**: 2026-03-19 16:55 CT  
**Target**: `sovran_ai.py`

---

## 🛑 P0: SL/TP Tick Calculation Inconsistency
- **Symptom**: `sl_ticks` and `tp_ticks` are calculated twice with different math in `calculate_size_and_execute`. 
- **Location**: `sovran_ai.py:1146-1165`
- **Impact**: One block uses points (wrong) while the other uses ticks. This will cause orders to trigger instantly or fail due to distance errors.
- **Fix**: Standardize to `int(pts / self.config.tick_size)`.

## 🛑 P1: Obsidian Order ID Mismatch
- **Symptom**: `finalize_trade` look for `"order_id"` but `active_trade` stores `"orderId"` or `"entry_order_id"`.
- **Location**: `sovran_ai.py:1555` vs `1198/1217/1242`.
- **Impact**: Trades will be logged with `trade_id=0` in Obsidian, breaking the learning link.
- **Fix**: Align all keys to `"order_id"`.

## 🟡 P2: Duplicate Imports & Unused Logic
- **Symptom**: `asyncio` and `logging` are imported multiple times. `_decode_ascii_codes_to_string` is likely dead code.
- **Location**: `sovran_ai.py:19-140`.
- **Impact**: Slight performance drag and technical debt.
- **Fix**: Cleanup imports and remove dead patches.

---
**Audit Complete.** Preparing hotfixes for P0 and P1 now.
